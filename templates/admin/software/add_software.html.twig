{% extends 'admin/layout.html.twig' %}

{% block body_id 'add_software' %}
    {% block main %}
        {{ form_start(form) }}
            {{ form_row(form.swName) }}
            {{ form_row(form.swUrl) }}
            {{ form_row(form.swDesc) }}
            {{ form_row(form.category) }}
            {{ form_row(form.subcategory) }}

        <button type="submit" class="btn btn-default pull-right" value="save">
            <label>Add Software</label>
        </button>
        {{ form_widget(form.saveAndCreateNew, {label: 'label.save_and_create_new', attr: {class: 'btn btn-primary'}}) }}

        {{ form_end(form) }}
    {% endblock %}

{#<button type="submit" class="btn btn-default pull-right" value="save">
            <label>Add Software</label>
        </button>
        <button type="submit" class="btn btn-default pull-left" value="saveAndAdd">
            <label>Save Add Software</label>
        </button>#}