{% extends 'admin/layout.html.twig' %}

{% block body_id 'update_locn' %}

{% block main %}
    <h1>Update Software Location</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">
            <label for="button">Update Location</label> <i class="fa fa-edit" aria-hidden="true"></i>
        </button>
    {{ form_end(form) }}
{% endblock %}
