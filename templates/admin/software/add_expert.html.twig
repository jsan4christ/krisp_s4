{% extends 'admin/layout.html.twig' %}

{% block body_id 'add_expert' %}

{% block main %}
    <h1>Add Software Expert</h1>
    {{ form_start(form) }}
      {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">
            <label>Add Expert</label> <i class="fa fa-plus" aria-hidden="true"></i>
        </button>
    {{ form_end(form) }}
{% endblock %}