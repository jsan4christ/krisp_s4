{% extends 'base.html.twig' %}

{% block body_id 'add_cat' %}

{% block main %}
    {{ form_start($form) }}
        {{ form_row(form.catName) }}
    {{ form_end($form) }}
{% endblock %}

{% block sidebar %}
        <div class="section">
            <a href="{{ path('view_cats') }}"><i class="fa fa-arrow-left"></i>View All Categories</a>
        </div>

{% endblock %}